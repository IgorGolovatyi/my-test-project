const usersSchema = require('./users.schema');
const newsSchema = require('./news.schema');
const favoritesSchema = require('./favorites.schema');

module.exports = [ usersSchema, newsSchema, favoritesSchema ];
