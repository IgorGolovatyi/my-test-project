const mongoose = require('./mongoose');
const express = require('./express');
const passport = require('./passport');
const newsapi = require('./newsapi');

module.exports = { mongoose, express, passport, newsapi };