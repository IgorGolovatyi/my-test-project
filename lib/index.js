const mongoose = require('./mongoose');
const passport = require('./passport');
const newsapi = require('./newsapi');
const сachе = require('./сachе');

module.exports = { mongoose, passport, newsapi, сachе };